<%- include('./partials/header') %>
<link rel="stylesheet" href="/css/viewbooking.css">
</br>


<main class="page-content">
   
     
      
  <%if (bookings.length >0){ %>
      <div class="container">
        <h3 style="text-align:center ;padding:30px;color:white;">Your Bookings</h3>
        <%- include('./partials/flashes') %>
        <div class="row">
            
         <!-- <table class="table table-striped table-primary"> -->
        
         <table class="table">
        
          <thead>
            <tr>
             
              <th>Name</th>
              <th>Mobile No.</th>
              <th>Date & TIme</th>
              <th>person</th>
              <th>Table No.</th>
              
              
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            
            <% bookings.forEach(function(booking){ %>
            <tr>
              
              <td><%= booking.name %></td>
              <td><%= booking.mobile %></td>
              <td><%= booking.time %></td>
              <td><%= booking.person %></td>
              <td><%= booking.table %></td>
              
              
              <td>
                
                <a class="btn btn-primary btn-xs" href="/edit_booking_<%= booking._id %>"><i class="fa fa-edit"></i></a>
                <a class="btn btn-danger btn-xs" href="/delete_booking_<%= booking._id %>"><i class="fa fa-trash"></i></a>
      
              </td>
            </tr>
            <tr>
              <% })}else{ %>
              <td colspan="6"></td>
              <td colspan="6"><a href="/addbook"><button btn.btn-primary></button></a></td>
              
              
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      </div>
    </main>
  
   <% if(bookings.length==0){%>
      <div class="container">
        <div class="row">
          <div class="col-sm-3">

          </div>
          <div class="col-sm-6">
            <div class="alert" style="background-color: yellowgreen;opacity:0.8"role="alert">
              <h5 style="color:white;text-align: center;">No Bookings Yet!</h5>
              </button>
            </div>
          </div>
          <div class="col-sm-3">

          </div>
          
        </div>

      </div>
    <%}%>
        
  
      
 
